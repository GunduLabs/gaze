# Channel metadata read by GunduLabs/packages (scripts/push_copr_builds.sh).
# Shipped next to the .src.rpm artifact so the publisher stays project-agnostic.
FORMAT=srpm
COPR_OWNER=gundulabs
COPR_PROJECT=gaze
COPR_CHROOTS=
