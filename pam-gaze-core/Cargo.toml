[package]
name = "pam-gaze-core"
version = "0.1.0"
edition = "2024"

[lib]

[dependencies]
gaze-core = { path = "../gaze-core", default-features = false }
libc = "0.2"
zbus = "5.15"
tokio = { version = "1.52", features = ["rt-multi-thread"] }
parking_lot = "0.12"
anyhow = "1.0"
futures = "0.3"
